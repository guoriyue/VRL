# SPRINT: repo hygiene sweep

Executing the outstanding items of `planned/SPRINT_full_repo_bloat_audit.md` §8.2,
one reviewable commit each, under `GOAL_repo_hygiene_sweep.md`.

`kept` is a finished outcome: the item was examined and deliberately left alone,
and the reason is the deliverable. `blocked` means it needs something this run
must not do (a GPU numerical gate, a public config-key migration).

## Ledger

| # | item | source | verdict | evidence | commit |
|---|---|---|---|---|---|
| 1 | three lazy-export facades -> shared installer | §8.2 | changed | All 46 table entries had the value duplicate its own key, and the `__getattr__`/`__dir__` bodies were identical apart from a docstring. Public surface verified unchanged before/after: `__all__`, the `AttributeError` text, and torch-free import all identical; `dir()` differs only by module-private names that were never in `__all__`. | (this commit) |
| 2 | `vrl/algorithms/grpo/__init__.py` as a fourth facade | §8.2 neighbour | kept | Not the same machinery: it deliberately exports nothing (`__all__ = []`, 10 lines) because `vrl.config.algorithm` imports each objective by submodule. Nothing to share. | — |
| 3 | sana `from_build` hand-copies the shared loader | §8.2 | changed | Its first six steps were the shared `DiffusersPipelineModelBase.from_build` verbatim; only the DPM->FlowMatchEuler swap and the fp16 saturation clamp are SANA's. Ten sibling families already opt in by declaring the three attributes, which the base docstring names as the intended form. New tests in `test_model_base.py` pin the swap (shift preserved), the encoder placement and the clamp, and **pass identically against the pre-refactor code**. | (this commit) |
| 4 | reward inference default-config construction sites | §8.2 | kept | The two `registry.py` fallbacks are not duplication to fold: each docstring states they are the boundary for direct `MultiReward` construction, where every component is in-process. `builders.py` resolves per name with `.get(name, default)` and `schema.py` parses the typed section — three different shapes, one shared meaning already stated in prose. Folding them would need a named constructor on `RewardInferenceConfig` with two callers, which the thin-function rule does not justify. | — |
<<<<<<< HEAD
=======
| 5 | cosmos3 replay `set_num_steps` override | §8.2 | changed | The pinned Cosmos3-Nano scheduler config (`use_dynamic_shifting: false`, revision the preset pins) makes the shared body exact, and `Cosmos3ReplayModel` already overrides `scheduler` to return its own. Identical situation to `CosmosPredict25ReplayModel`, whose override the audit removed with the same comment. A new cross-family guard covers 15 replay classes and goes red when the scheduler property is pointed at the pipeline. | (this commit) |
| 6 | `tests/generation/execution/test_worker_sleep.py::test_real_cumem_one_shot_scope_sleep_wake_in_subprocess` fails under a concurrent GPU job | observed | blocked | Passes in isolation; fails in a full run while another process allocates on the card, because it bounds residual with a device-wide `mem_get_info`. Same root cause as the parking gate fixed in `f24d076f`, which now reads the driver's per-process table. Not in the audit queue and not caused by this sweep; recorded so the next run does not chase it as a regression. | — |
| 7 | wan `WanI2VReplayModel` MRO | §8.2 | changed | It restated five members of `WanT2VReplayModel` verbatim and hand-delegated two more, with a comment saying inheritance was avoided to keep the I2V forward math. Listing the t2v replay first and the i2v wrapper second gets both: an attribute-ownership diff over all 146 members shows exactly those seven moving and no forward method changing hands. Guarded in the wan loading tests, which go red when the inheritance is removed. | (this commit) |
| 8 | `retain_artifacts` | §8.2 | kept | Not scaffolding: it is a live config knob reached as `reward.kwargs.<name>.retain_artifacts`, and `tests/e2e/test_real_checkpoint_rl.py` sets it to keep reward artifacts for inspection. Removing a public config key is outside this sweep. |
| 9 | reward service `_execute` / `_revalidate_cached` | §8.2 | kept | Both are live handlers in `vrl/rewards/service/server.py` (the scoring path and the idempotent-replay path), called from the same dispatch. Only their `CancelledError` block is byte-identical; the other two handlers differ by error code and log line -- SCORING_FAILED versus INTERNAL_ERROR is the semantic difference that justifies two bodies, so folding them would delete the argument rather than the duplication. The audit rated this low value and it is. |
| 10 | scripts long-tail dead CLI flags | §8.2 | kept | The gate the audit installed answers it: `vrl.scripts.lint.dead_flags` reports **435 declared flags, all with a consumer** (350 at the audit's close). There is no long tail left to sweep. |
| 11 | `--vbench-*` decision | §8.2 | kept | `vbench==0.1.5` is a declared extra in `pyproject.toml`, deliberately isolated because it hard-pins `transformers==4.33.2`; the flags are consumed at `video_reward_suite.py:150-160`, and absence degrades to empty `vbench_*` columns plus a warning rather than an error. Live and already designed for the missing-extra case. |
| 12 | `init-dirs` | §8.2 | kept | Consumed by `tests/data/test_artifact_manifest_validation.py` and documented as a `vrl.scripts.data.setup` subcommand; its directory table was examined and kept by the ALL_CAPS audit for the same reason. |
<<<<<<< HEAD
>>>>>>> 613b9bba (docs(sprints): record five audit items examined and deliberately kept)
=======
| 13 | `_OFFLINE_DPO_*_FIELDS` cross-validation | §8.2 | changed (actor half) | The audit asked for derivation or a cross-check; derivation is out of scope because the list *is* the recipe's public config surface. A runtime cross-check instead: instrument attribute access on the actor section across the recipe's two pure-config resolvers and assert every allow-listed name is read. Adding an unread name to the list turns it red. Writing it found that a builder-only version reports a false positive on `gradient_checkpointing`, which `train_dpo.py` reads through `enable_transformer_gradient_checkpointing` -- the field is live and stays. The trainer half is left as-is: all seven of its names are read as plain `trainer.<name>` attributes and a symbol grep finds them, so they carry none of the `required("name")` rot risk. | (this commit) |
| 14 | precision `_select` -> `cfg_path` | §8.2 | kept | Resolved upstream: `fab177b7` ("delete the duck-typed accessors; every reader takes the parsed root") removed both symbols. Neither `_select` nor `cfg_path` exists in `vrl/` any more. |
| 15 | `model.lora.init` / `init_lora_weights` dual alias | §8.2 | blocked | Removing an alias is a public config-key migration, which this sweep must not do. |
<<<<<<< HEAD
>>>>>>> d0c87069 (test(config): the offline-DPO actor allow-list must have a reader per field)
=======
| 16 | kling / unified_reward_video 两份 reward function 测试文件 | §8.2 | changed | The deferral read them by their call sites; the bodies say otherwise. All four wrappers (hpsv3, videoscore2, kling, unified) are zero-method subclasses that pin only `model_factory`, `debug_basename` and defaults, and the two theorems held to be kling-specific -- release-after-success and the empty-runtime `result/artifact mismatch` -- are raised by `vrl/rewards/base.py` and `vrl/rewards/inference.py`, so they were base-class behavior anchored on one arbitrary subclass. Both become parametrized theorems over four cases; `test_function.py` is fully covered and deleted, and the unified module keeps only its own axis-parsing and rubric theorems (which really are unique, and mirror `videoscore2/test_parsing.py`). `test_versioning.py` stays: the debug-row schema is a distinct theorem, not a duplicate. Negative control: flipping `retain_artifacts` and the logged version string turns 8 of 20 cases red. | 83a00b0c |
| 17 | generation weight_sync remote-vs-local dual path (2-A) | §8.2 | changed | `push_to_rollout_engines` probed each engine for a callable `.remote` and otherwise called the rank directly, in a branch whose own comment read "Local test double". Unreachable in production: the sole construction site builds engines from a `RayActorGroup`'s handles and the same function already calls `.remote()` on them unconditionally. Its only two drivers asserted theorems the remote and real-Ray tests already carry. Deleting it exposed a real gap the other way -- the non-integer ACK path had no test at all -- so it gained one, with a negative control. | 83399f5f |
| 18 | generation executor remote-vs-local dual paths (2-A core) | §8.2 | changed | The deferral said this needed the doubles migrated onto a package-level real Ray cluster. It did not: `test_pipelined_progress.py` was already driving this same executor through hand-rolled `.remote()` faces, so the fix is the audit's own stated one -- give the double the actor's method face. `RemoteFace` in `tests/generation/ray/_helpers.py` wraps a synchronous worker over the existing `ResolvedRef`, raising on await like a real ObjectRef and passing every other attribute through. Instrumenting the four else-branches with a raise showed the whole blast radius is 10 tests in 2 files. All four branches deleted; the two `.remote` probes left in the file both raise, which is the shape the audit endorsed. | 69635fa3 |
>>>>>>> d8de8705 (docs(sprint): record the last three hygiene-sweep verdicts)

## Notes for the reviewer

- The whole-repo audit's conclusion stands: there is no structural rot. These
  items are the ones its execution batches deliberately deferred, each with a
  recorded reason.
- One test failure is pre-existing on this machine and unrelated:
  `tests/nn/kernels/test_vllm_paged_attention_real_ops.py` fails by upstream
  design here.

## Fresh pass: `vrl/trainers/` and `vrl/scripts/`

The §8.2 queue above is closed. The goal's next stage is the two packages the
full-repo audit rated worst. Two read-only reconnaissance passes produced
candidate lists; every item below was re-read and verified before acting, and
the ones that survived reading as justified are recorded as `kept` with the
reason so they are not re-discovered.

| # | item | verdict | evidence | commit |
|---|---|---|---|---|
| 19 | `vrl/generation/ray/{runtime,session,health_monitor}.py` tolerate rank actors with no method face | changed | Same disease as the executor branches, one layer up. `runtime.py` said so in a comment: "Tolerate probe-less test doubles the way the health monitor does". Instrumenting the preflight and teardown skips with a raise and running all 3578 tests produced zero hits, and production cannot reach them (every rank is a `RayGenerationWorker`, which defines both methods). The monitor's skip had one driver: a test asserting the skip. It cannot raise on its own daemon thread, so the probe is now unconditional and the `AttributeError` terminalizes through `RolloutWorkerUnreachable` -- a fleet of face-less actors used to be reported as a healthy fleet. | `6999343a` |
| 20 | eleven hand-rolled fake `ObjectRef` classes across five Ray test modules | changed | Three behaviors (resolve now / never / on a gate) with a copy each per module. `_helpers.py` already owned the first as `ResolvedRef`; the other two join it, and `GatedRef` raising an exception value subsumes the separate raise-on-gate copy. Three copies stay for stated reasons: two carry per-test semantics (a scripted behaviour, an event set from inside the await) and `tests/ray/test_batch_dispatch.py`'s are a controlled clock whose `NeverRef` busy-spins the loop on purpose. | `84a82c3e` |
| 21 | `EMAModuleWrapper.state_dict` hands back the live shadow tensor | changed (bug) | Its two checkpoint paths disagreed: `checkpoint_state_dict` gathered, detached, moved to CPU and cloned; `state_dict` did that only for DTensor shadows and returned a plain shadow as-is, uncloned and on its live device. A snapshot taken through it kept moving as training continued, and the same checkpoint written through the two paths differed in device placement. This is the hazard `fsdp.py` already documents for the optimizer gather; both paths now share one `_snapshot(parameter, keep=...)` shaped like that module's `_full_cpu_tensor`. Negative control: returning the live tensor reddens both cases. | `f5ff2893` |
| 22 | `getattr(ema, "checkpoint_state_dict", None)` in the trainer | changed | Can never be absent: `self._ema` is typed `EMAModuleWrapper | None`, `_ensure_ema` is its only producer, and the class declares the method unconditionally. The structurally identical probe in `checkpointing.py` stays -- `OfflineDPOTrainer` really defines only `state_dict`, so that one is polymorphic. | `e52690d4` |
| 23 | `gather_trainable_state_dict` docstring claims to be the checkpoint gather | changed (docs) | It is only the rollout weight-sync gather; the checkpoint model gather is `gather_checkpoint_state_dict`, which hardcodes `keep=True`. So `rank0_only` here is test-only. Kept and annotated rather than deleted: it is the same seam `gather_full_optimizer_state_dict` already gives checkpoint export, and it is where the fix goes for the asymmetry this exposed -- optimizer state is gathered rank0-only for checkpoints, model state is still retained in full on every FSDP rank. | `59ef7c6c` |
| 24 | two disagreeing precision-label normalizers | changed | The trainer's mapped `no`/`float32`/`bfloat16`/`float16` and stripped `torch.`; the guard's mapped only `no` and `""`, then ran a second time on already-normalized input. Reachable with a raw value, because the guard is public API and `quantized_rollout_drift_probe.py` calls it directly -- `torch.bfloat16` and `"bf16"` for two equal roles would have compared unequal and armed the guard. The complete one moves to `precision_guard.py`, which owns the comparison. | `2ab562da` |
| 25 | `resolved_config.yaml` hardcoded in two eval entrypoints | changed | `RESOLVED_CONFIG_NAME` is owned by `checkpointing.py` and imported by every other checkpoint-eval script; these two built three paths from the literal, one of them the provenance sha256. | `3c0b4bee` |
| 26 | 32 source-less `__pycache__` directories under `vrl/` and `tests/` | changed | Residue of past module moves (`vrl/rewards/evaluation`, `tests/models/diffusion/*`, ...). All untracked, no `.py` anywhere below them, no importers -- but `vrl/rewards/evaluation` was still importable as an empty namespace package. Removed from the working tree; nothing tracked changed, so there is no commit. | — |
| 27 | `DDPStrategy.export_rollout_state` hand-copies the single-process one | changed | Same body: unwrap, select requires-grad keys against the unwrapped `state_dict`, refuse empty, non-aliasing CPU snapshot. DDP's extra `.detach()` pass was already done by `to_cpu_snapshot`, and `select_trainable_state` only reads keys. `_UnshardedStateStrategy` is the owner -- its docstring already states the precondition that makes both correct and already names FSDP as the override -- so it goes from five shared methods to six. The copy's real content was a 2x1 NCCL postmortem, left half-edited by a stalled earlier merge; it survives on the shared method. The existing DDP-vs-single-process equality test was the safety proof and would now be a tautology, so it splits into a resolution test and the end-to-end check. | `040e765d` |
| 28 | `build_trainable_state_sync_getter` | changed | A closure factory whose only production caller invoked it on the same line, so the deferral never happened; after row 27 it had test callers only. Its docstring content moves onto `flatten_trainable_module_state` and its test moves with it, gaining the half it only claimed (write a new value into the live module, see the next flatten report it). The `TrainableStateGetter` alias stays -- live protocol type on the trainer's `sync_state_getter`. | `3c53e862` |
| 29 | six JSON report writers, three of them non-atomic | changed (bug) | Three identical non-atomic one-liners, two independently written atomic versions that had diverged on temp-file strategy and NaN strictness, and `write_report` in the dataset scripts. It matters: `wan_hpsv3` and `wan_robotics` each write a provenance plus a report that a later `score` subcommand re-reads and integrity-checks, and a truncated report is indistinguishable from a complete one. `write_json_report` now lives beside `sha256_file` in `vrl/utils/artifacts.py`, which that module already calls the single file-integrity implementation shared across domains. One deliberate tightening, `allow_nan=False`. Four tests, each with its own negative control. | `07c84794` |
| 30 | `sana_checkpoint_compare._sha256` alias | changed | The recon said test-only; reading showed three production uses in the same file. The alias still goes: its comment claimed it existed to keep a test reference resolving and that the canonical digest lives in `vrl.rewards.inference`, and both halves were false. | `97c3f193` |
| 31 | `robotics_reward_verdict(policy=None)` | changed | `RoboticsRewardGatePolicy` has no importer outside its module and no caller passes it, so the parameter was always `None`. The struct stays -- seven numbers that are one pre-registered bar, recorded in the verdict under a `policy` key, which is also why a caller override was the wrong seam: it would let a run report PASS against a bar nobody registered. | `4023b75e` |
| 32 | `wan_i2v_base_sample --baseline-dir` default | changed | Pointed at `outputs/wan_phys_eval_AB/baseline`, which does not exist and which nothing in the repo produces, so running with defaults failed as a missing file rather than a missing argument. No canonical location to repoint to, so the flag is required, which is what the docstring already said. The script stays: a parked sprint is still waiting on the run it performs. | `a2f4074e` |
| 33 | `ResolvedArtifact` / `source_episode_overlap` | kept (annotated) | No non-logging consumer: `to_dict` and tests only, and validation raises inside the resolution loop before an instance exists. Kept and annotated `display/provenance-only` at the definitions, which is what the dead-field rule asks for when a payload carries un-derivable provenance -- the raw-string-to-absolute-path mapping under a given data root cannot be re-derived once either moves. | `37e3f353` |
| 34 | `Any` on the replay sweep's parameters | changed | `_run_replay_pass` erased four types that are all reachable in the same file, one of which (`AlgorithmAdapter`) another method in that file already spells; same for `_sample_batch_train_indices`. None carried the import-cycle or raw-input receipt an `Any` is supposed to. | `8ccd179f` |
| 35 | the denoise step written twice inside the shared perf runtime | changed | `diffusion_runtime.py` exists to be the shared runtime for the real diffusion perf probes, and the ten-line step appeared in both the timing closure and the end-to-end path -- so two probe families could drift on what "one production denoise step" means while both claiming to measure it. Now `denoise_step`, with each caller keeping only what differs. | `6cc8bc39` |
| 36 | three reward worker-config projections | changed | `_kling_reward.py` already declared itself the single owner and two other scripts had their own; `wan_hpsv3`'s read the reward block through raw `OmegaConf.select` rather than `RewardRuntimeConfig`, so two entrypoints read the same thing by different means. The recon's open question -- whether the shared path resolves interpolations -- is answered by `RewardConfig.from_cfg`, which does, with `throw_on_missing=True`, and which also already rejects a non-mapping `reward.kwargs.<name>` by config path. Module renamed to `_reward_worker.py` because it is no longer kling-specific. The stricter upstream validation wants the whole reward section, which only exposed an under-specified test fixture. | `a9620408` |
| 37 | `load_resolved_run_config` re-implemented for want of an `overrides` parameter | changed | It calls itself the one reader for every checkpoint-evaluation entrypoint; `wan_hpsv3` could not use it because it evaluates with compile off and the LoRA path cleared. `overrides` is now a parameter applied before parsing. `sana_aesthetic_checkpoint_eval` keeps its own: it runs `normalize_run_config` *between* load and parse, which is a step inside the function, not a parameter of it. | `ee1c9ce3` |
| 38 | two identical `--checkpoint [LABEL=]PATH` parsers | changed | Same body, same uniqueness wrapper, same error strings, same declared CLI contract; the only difference was whether a plain file is acceptable. Folded onto `CheckpointTarget`, which is what they both produce and which already owns `base()` and `load()`. `image_checkpoint_eval.load_target` looks like a third copy and is not -- it rejects an unusual label instead of sanitizing it, strips a LORA weights filename to its directory, and pins through `CheckpointTarget.load` with a digest. | `428518fe` |
