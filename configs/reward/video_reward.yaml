# Video-level reward for world-model RL. Stub is explicit and only validates
# the training chain until the Ray reward inference runtime is implemented.
reward:
  components:
    video_reward: 1.0
  kwargs:
    video_reward:
      backend: stub
      reward_name: dance_grpo
      score_key: overall_reward
      media_type: video
      timeout_s: 60.0
      debug_dir: ""
      stub_scale: 0.01
