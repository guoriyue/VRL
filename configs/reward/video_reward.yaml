# Video-level reward for world-model RL.
reward:
  components:
    video_reward: 1.0
  kwargs:
    video_reward:
      inference_runtime: ray
      reward_name: dance_grpo
      score_key: overall_reward
      media_type: video
      artifact_dir: outputs/reward_artifacts
      timeout_s: 60.0
      debug_dir: ""
