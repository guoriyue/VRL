# Janus-Pro-1B + Janus-Pro-R1-style OCR GRPO mechanism-level run.
defaults:
  - /recipe/online/ar_multisegment_token_grpo
  - /model/ar/janus_pro/1b
  - /sampling/ar/r1_image_384_576tok
  - /reward/ocr
  - /dataset/ocr

model:
  # The checkpoint is shared with Janus-Pro, but R1 selects a distinct
  # executor/gatherer/trajectory contract and must name that registry entry.
  family: janus_pro_r1

actor:
  optim:
    lr: 1.0e-4

trainer:
  output_dir: outputs/janus_pro_1b_r1_ocr_grpo
  total_epochs: 80
  save_freq: 10
  debug:
    # Multi-segment old log-probs are kept per segment; the legacy first-step
    # scalar parity check is for single-trajectory token GRPO only.
    first_step: false

rollout:
  n_samples_per_prompt: 2
  prompts_per_batch: 1
