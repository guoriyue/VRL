# Aesthetic scorer with an explicit optional Hub revision boundary.
#
# The revision pins the SigLIP encoder; the V2.5 head is packaged with VRL.
defaults:
  - /reward/aesthetic
  - _self_

reward:
  components:
    aesthetic: 1.0
  kwargs:
    aesthetic:
      model_revision: null
