# VideoCon-Physics reward (videophysics/videocon_physics, mPLUG-Owl-Video 7B).
# Custom architecture: model code is vendored under third_party/mplug_owl_video/.
# Scores Yes/No entailment for "Does the video follow physical commonsense?".
distributed:
  resources:
    reward:
      num_gpus: 1
      gpus_per_worker: 1.0
      num_workers: 1

reward:
  components:
    videocon_physics: 1.0
  kwargs:
    videocon_physics:
      reward_name: videocon_physics   # logical tag (request label)
      score_key: physical_commonsense
      media_type: video
      artifact_format: mp4
      artifact_dir: ${trainer.output_dir}/reward_artifacts
      debug_dir: ${trainer.output_dir}/reward_debug
      # Omit model_path to resolve reward_model_name from the local HF
      # cache; set worker_config.model_path to load a local checkpoint dir.
      worker_config:
        reward_model_name: videophysics/videocon_physics@main   # which HF checkpoint to load
        dtype: bfloat16
        num_frames: 32
