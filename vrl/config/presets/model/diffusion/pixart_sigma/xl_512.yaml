# PixArt-Sigma XL-2 512-MS (epsilon DDPM DiT + SDXL KL-VAE) — checkpoint, LoRA.
# Non-flow family: pair with rollout.sde.type: ddim (the family rebuilds a
# DDIMScheduler from the shipped DPM-Solver beta config for RL log-probs).
model:
  family: pixart_sigma
  executor:
    max_sequence_length: 300
  path: PixArt-alpha/PixArt-Sigma-XL-2-512-MS
  revision: e102b3591cc82e97071b8b4cb90d834d0c487207

  use_lora: true
  lora:
    rank: 16
    alpha: 32
    target_modules:
      - to_k
      - to_q
      - to_v
      - to_out.0

  torch_compile:
    enable: false
    mode: default
