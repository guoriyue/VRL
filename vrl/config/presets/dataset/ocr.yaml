# OCR prompt manifest. load_prompt_manifest extracts the target from quoted
# text in each prompt row.
data:
  loader: prompt_manifest
  manifest: datasets/ocr/train.txt
  eval_manifest: datasets/ocr/test.txt
  preprocessing:
    format: text
  sampler:
    type: random_without_replacement
