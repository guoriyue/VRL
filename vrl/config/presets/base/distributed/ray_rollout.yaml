# Ray rollout preset: auto split. Resource resolution defaults do the work —
# the trainer takes one visible GPU and the rollout auto pool takes the spares
# (spare-first-else-share). Pin `devices` per role to override placement.
distributed:
  rollout:
    cpus_per_worker: 4.0
