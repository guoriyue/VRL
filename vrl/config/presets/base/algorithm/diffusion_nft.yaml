# DiffusionNFT-style RL objective for diffusion world models.
algorithm:
  kind: diffusion_nft
  eps: 1.0e-8
  adv_clip_max: 5.0
  global_std: false

  # Cosmos-RL DiffusionNFT parity knobs.
  nft_beta: 1.0
  kl_coef: 1.0
  advantage_scale: 5.0
  weight_copy_decay: 0.0
