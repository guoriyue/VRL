# Default rollout orchestration: collect one rollout, train it, then sync.
# Admission and weight-sync ordering are owned by the strict schedule state
# machine; there is no independent queue-capacity or barrier knob.
trainer:
  rollout_orchestration:
    schedule_mode: strict_on_policy
